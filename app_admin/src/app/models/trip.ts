export interface Trip {
    _id: string;         // _id is required
    code: string;
    name: string;
    length: string;
    start: string;
    resort: string;
    perPerson: number;    // Change perPerson to number
    image: string;
    description: string;
  }
  